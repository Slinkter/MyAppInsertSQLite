<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vidrieriachaloreyes.myappcrudsqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Business.DetalleMascotaActivity"></activity>
        <activity android:name=".Business.ListaMascotaActivity" />
        <activity android:name=".Business.RegistroMascotaActivity" />
        <activity android:name=".Business.DetalleUsuarioActivity" />
        <activity android:name=".Business.ConsultarListaViewActivity" />
        <activity android:name=".Business.ConsultaComboActivity" />
        <activity android:name=".Business.ConsultaActivity" />
        <activity android:name=".Business.RegistrerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>